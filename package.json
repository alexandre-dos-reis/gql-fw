{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "pnpm exec dotenv -- turbo dev",
    "build": "pnpm exec dotenv -- turbo build",
    "migrate": "pnpm exec dotenv -- pnpm --filter database run migrate",
    "codegen:graphql": "pnpm exec dotenv -- turbo codegen:graphql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.10.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "dotenv-cli": "^7.3.0"
  }
}
